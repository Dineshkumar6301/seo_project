name: Daily Rank Check

on:
  schedule:
    - cron: '0 5 * * *'

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Rank Check
        run: |
          curl https://https://digital-marketing-q6hei.ondigitalocean.app/api/run-rank-check/
