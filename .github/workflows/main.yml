name: Daily Rank Check

on:
  schedule:
    - cron: '0 5 * * *'   # 10:30 AM IST
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Rank Check
        run: |
          curl -L https://digital-marketing-q6hei.ondigitalocean.app/api/projects/api/run-rank-check/
