name: Daily Rank Check

on:
  schedule:
    - cron: '30 6 * * *'   # 12:00 PM IST
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Rank Check
        run: |
          curl -v -L https://digital-marketing-q6hei.ondigitalocean.app/api/projects/api/run-rank-check/